import babel from "rollup-plugin-babel";
import { uglify } from "rollup-plugin-uglify";

export default {
    input: "./src/index.js",
    output: {
        file: "./build/dataframe.min.js",
        name: "dfjs",
        format: "umd"
    },
    plugins: [
        babel({
            exclude: "node_modules/**"
        }),
        uglify()
    ]
};
